# M02 项目信息与工程阶段管理 — 代码生成阶段测试报告

- 任务项：M02；阶段：代码生成
- 测试依据：`.specs/M02/需求分析/测试案例.json`（M02-TC-01…M02-TC-45，用例 id 314–358）、`.specs/M02/技术设计/通用详细设计文档.md`（设计单元 D-M02-01…D-M02-12、决策 DD-M02-01…DD-M02-14）
- 被测构建：`target/coreagent-0.1.0-SNAPSHOT.jar`（`mvn package` BUILD SUCCESS，2026-09-22 08:47；含全部本次改动）
- 验证结论：**45 项用例全部执行，40 项通过，5 项为「本模块侧通过 + 跨模块侧待联调」（M10/M27），0 项失败**

---

## 1. 验证环境与数据基线

| 项目 | 取值 |
|---|---|
| 运行环境 | JDK 17（`C:/Program Files/Java/jdk-17`）、Spring Boot 3.5.13、Maven 3.9.15 |
| 服务启动 | `java -jar target/coreagent-0.1.0-SNAPSHOT.jar`，Tomcat 18080 启动成功（08:48:06），HikariPool 连接池建立成功 |
| 数据库 | MySQL 8.0.30 `jdbc:mysql://ecs:3306/coreagent`（严格模式），`spring.jpa.hibernate.ddl-auto=none` |
| 前端 | 无构建 ES Module 静态资源，经 DOM 垫片在 Node 中真实挂载 `pages/ProjectInfo.js`、`layout/AppHeader.js` |
| 主题门禁 | `ThemeHardcodeScanTest` 通过（`src/main/resources/static/**` 无硬编码色值） |

**真实数据基线（验证前后逐项核对，未被改动）**

- `project_info` 2 行：id=1 `xx银行新一代核心系统建设`（updated_at `2026-08-28 19:32:03`，阶段含 `交付归档`）、id=2 `平台迭代优化`（updated_at `2026-09-18 09:11:29`）
- `agent_definition` 44 行，分类分布：交互对话类 11、内容提取类 9、代码生成 8、需求分析 6、通用 3、技术设计 2、脚本生成类 2、交付归档 1、测试验证 1、规划设计类 1
- `agent_category` 5 行（交互对话类/内容提取类/脚本生成类/规划设计类/通用）
- 会话类表（`ai_conversation` 106 行、`ai_message` 194 行、`agent_context` 69 行）整表指纹在验证前后一致

破坏性用例（TC-27、TC-43）与临时数据用例（TC-44、TC-45、跨模块契约用例）均在结束时清理，并以「基线校验」用例逐项断言还原结果。

## 2. 验证方法

| 层 | 手段 | 覆盖用例 |
|---|---|---|
| 接口 + 真实库 | Node 脚本直连服务，JDBC 校验落库结果 | 01–12、14–20、24–28、37、38 |
| 破坏性构造 | 清空 `project_info` 全表后验证，快照脚本回放还原 | 27、43 |
| 前端交互 | 极简 DOM 垫片上真实挂载构件，桩化 `http.js` 下游 | 13、29–32、35、36、39–42、45 |
| 端到端（真实库） | 临时项目/Agent 行构造，验证回落保存与阶段替换 | 44、45 |
| 跨模块契约侧 | 真实库 + 真实接口，验证本模块侧的取数与「无写入」 | 21、22、23、33、34 |

验证脚本与结果文件位于 `target/m02tools/`（`verify*.mjs` / `verify*-result.json`，非交付物）。

## 3. 逐项验证结果

### 3.1 需求条款 RC-M02-01…RC-M02-08（项目定义与阶段规则）

| 用例 | 条款 | 方向 | 状态 | 关键证据 |
|---|---|---|---|---|
| M02-TC-01 | RC-M02-01 | 反向 | PASS | 空名与仅空白名均拒绝，返回 `M02-E001`，`project_info` 无写入 |
| M02-TC-02 | RC-M02-01 | 正向 | PASS | 前后空白裁剪后保存，落库值为裁剪结果 |
| M02-TC-03 | RC-M02-02 | 正向 | PASS | 128 字符名称保存成功 |
| M02-TC-04 | RC-M02-02 | 反向 | PASS | 129 字符拒绝，返回 `M02-E002` |
| M02-TC-05 | RC-M02-01/02 | 正向 | PASS | 原始 132 字符（含 4 空白）裁剪后 128 字符保存成功，按裁剪后长度判定 |
| M02-TC-06 | RC-M02-03 | 正向 | PASS | 目标与范围裁剪后落库，空值存 NULL |
| M02-TC-07 | RC-M02-03 | 反向 | PASS | 清空后接口返回 `null`，页面展示为空且不报错 |
| M02-TC-08 | RC-M02-04 | 正向 | PASS | 阶段落库为英文逗号分隔文本，无中文逗号/空格残留 |
| M02-TC-09 | RC-M02-05 | 反向 | PASS | 存量 `NULL`、`''`、`','` 三种形态读取均回落默认五阶段；前端按下标渲染 01…05 且顺序一致 |
| M02-TC-10 | RC-M02-06 | 正向 | PASS | 去重、丢弃空白项、保持输入顺序 |
| M02-TC-11 | RC-M02-07 | 反向 | PASS | 缺失单个锁定阶段拒绝，`M02-E003`，数据无变化 |
| M02-TC-12 | RC-M02-07 | 反向 | PASS | 缺失多个锁定阶段时提示**列出全部**缺失项（`；` 连接） |
| M02-TC-13 | RC-M02-08 | 正向 | PASS | 锁定行渲染只读文本（`.stage-name-locked`，`font-weight: var(--ca-font-weight-bold)`），无输入框、无删除按钮；只读行由服务端 `lockedStages` 决定，前端不复制常量 |

### 3.2 需求条款 RC-M02-09…RC-M02-14（Agent 归属迁移与协作）

| 用例 | 条款 | 方向 | 状态 | 关键证据 |
|---|---|---|---|---|
| M02-TC-14 | RC-M02-09 | 正向 | PASS | 1 删 1 增判定为改名，`agent_definition.category` 由旧名整体迁移到新名，迁移后旧分类清空 |
| M02-TC-15 | RC-M02-09 | 反向 | PASS | 仅删除阶段不触发迁移，Agent 归属保持原分类 |
| M02-TC-16 | RC-M02-10 | 正向 | PASS | 阶段名与分类名严格同名（无截断、无改写），旧分类名不再存在 |
| M02-TC-17 | RC-M02-11 | 反向 | PASS | 阶段增删前后 `agent_category` 记录集合不变（未随阶段写表） |
| M02-TC-18 | RC-M02-12 | 正向 | PASS | 项目列表按 `id ASC` 返回（`findAllOrdered` 已改序） |
| M02-TC-19 | RC-M02-13 | 正向 | PASS | 携带 `X-Project-Id` 时按该 ID 定位更新，其余项目字段与 updated_at 不变 |
| M02-TC-20 | RC-M02-13 | 正向 | PASS | 未携带时更新 ID 最小的首个项目 |
| M02-TC-44 | RC-M02-09/10/11 | 正向 | PASS | 一次保存中删除「代码评审」并新增「代码审查」：2 个 Agent 归属全部迁移、项目 stages 与分类名严格同名、`agent_category` 记录集合不变、其余 42 个 Agent 归属未受波及（M10 分类列表渲染属 M10，见 §5） |

### 3.3 需求条款 RC-M02-14…RC-M02-20（会话取数契约与写路径收敛）

| 用例 | 条款 | 方向 | 状态 | 关键证据 |
|---|---|---|---|---|
| M02-TC-21 | RC-M02-14 | 反向 | PARTIAL | M02 侧：一次含名称/目标/范围/阶段的真实保存后，`ai_conversation`、`ai_message`、`agent_context` 整表指纹（行数+逐行 CRC32+总长度）完全不变——既有会话快照由「不存在写入路径」保证（D-M02-07/C-05）。M27 侧「重新打开既有会话仍为快照」待 M27 就绪 |
| M02-TC-22 | RC-M02-15 | 正向 | PARTIAL | M02 侧：`GET /api/projects/current` 下发目标 `建设新一代核心存款系统`、范围 `覆盖存款、账户与利息域`、名称与阶段数组，取数路径为设计指定的项目明细端点（D-M02-07/C-06，未改动 M01 `GlobalContextVO`）。M27 侧注入组装待联调 |
| M02-TC-23 | RC-M02-16 | 反向 | PARTIAL | M02 侧：读取响应字段集合恰为 `id/name/goal/scope/stages/lockedStages/updatedAt`，不含需求规格关联等人工维护字段；`project_info` 无相关列，无可泄露来源。M27 侧注入内容待联调 |
| M02-TC-24 | RC-M02-17 | 反向 | PASS | 真实故障注入：目标分类名 24 字符 > `category varchar(16)`，MySQL 拒绝（`Data too long for column 'category'`）→ 整体回滚，返回 `M01-E006` 503，阶段、名称、目标与 `agent_definition` 全部保持变更前状态 |
| M02-TC-25 | RC-M02-18 | 正向 | PASS | 新增仅写 `project_info` 一行且主键自增；`agent_definition` 行数不变；无组件/工作流写路径 |
| M02-TC-26 | RC-M02-19 | 正向 | PASS | 未传 `stages` 时按默认五阶段序列落库 |
| M02-TC-27 | RC-M02-20 | 正向 | PASS | 清空全表后：`GET /api/projects`、`GET /api/context` 均**不**创建默认行（DD-M02-03 写路径收敛）；`GET /api/projects/current` 在同一事务内创建默认项目行（名称「AI新一代核心系统」、目标与范围为空、默认五阶段）并返回；无项目时 `PUT /api/projects` 返回 404 `M01-E003` 且不创建；空请求体不改动任何字段 |

### 3.4 需求条款 RC-M02-21…RC-M02-28（错误分支、属性完整性、派生信息）

| 用例 | 条款 | 方向 | 状态 | 关键证据 |
|---|---|---|---|---|
| M02-TC-28 | RC-M02-21 | 反向 | PASS | 携带不存在的项目 ID：404 `M01-E003`，文案「项目不存在：999」，**不**回落到首个项目；按 ID 读取同码同文案（TC-28b） |
| M02-TC-29 | RC-M02-22 | 正向 | PASS | 保存成功后顶栏「当前项目」文本立即由旧值变为新值，未依赖页面刷新 |
| M02-TC-30 | RC-M02-22 | 正向 | PASS | 保存成功后会话态 `projectGoal`/`projectName`/`stages` 与本次提交及服务端返回一致；`projectName` 与顶栏同源 |
| M02-TC-31 | RC-M02-23 | 正向 | PASS | M02 提供名称、目标、范围与阶段四项取值（文案与注入格式属 M27，DQ-M02-03） |
| M02-TC-32 | RC-M02-23 | 反向 | PASS | 目标为空时字段值为 `null`，供 M27 按空值跳过规则处理（D-M02-07/C-02） |
| M02-TC-33 | RC-M02-24 | 正向 | PARTIAL | M02 侧：新增非锁定阶段「代码评审」保存成功，阶段序列立即含该阶段（M10 派生分类列表的唯一来源），`agent_category` 未被写入。M10 分类列表渲染待联调 |
| M02-TC-34 | RC-M02-24 | 反向 | PARTIAL | M02 侧：删除「代码评审」保存成功，阶段序列不再含该阶段，无残留分类数据。M10 分类列表渲染待联调 |
| M02-TC-35 | RC-M02-25 | 反向 | PASS | 读取接口 503 时展示失败块（含响应体 message）、提供重试入口、页面头部保留、不渲染空白表单、不白屏；点击重试可恢复页面 |
| M02-TC-36 | RC-M02-26 | 反向 | PASS | 保存失败时项目列表未新增记录、会话态名称与目标未被改写、浮动提示失败原因、页面内容不丢失 |
| M02-TC-37 | RC-M02-27 | 正向 | PASS | 响应含 `id/name/goal/scope/stages/updatedAt`，无 `created_at`；`updated_at` 由系统自动更新，不要求人工传入 |
| M02-TC-38 | RC-M02-28 | 正向 | PASS | `project_info` 无阶段序号/数量列，接口无阶段数量字段，序号由数组顺序派生 |

### 3.5 需求条款 RC-M02-29…RC-M02-32（页面构件与顶栏）

| 用例 | 条款 | 方向 | 状态 | 关键证据 |
|---|---|---|---|---|
| M02-TC-39 | RC-M02-29 | 正向 | PASS | 标题「项目信息」、说明文案、提示条文案与需求定值逐字一致；主操作「保存配置」；名称输入框 `maxlength=128`；目标与范围为多行文本；含阶段表格；面包屑「环境管理 / 项目配置」 |
| M02-TC-40 | RC-M02-30 | 正向 | PASS | 「新增阶段」位于表格上方；每行含「删除阶段」；序号两位补零（01…05）且删除后连续；删除为纯本地操作（不发起请求） |
| M02-TC-41 | RC-M02-31 | 反向 | PASS | 校验失败以浮动提示（`.toast-root`，`role=status`，非模态）给出文案且不阻断页面其它操作、不发起请求；服务端 `M02-E003` 亦以浮动提示展示完整缺失清单，页面停留在当前状态，主操作恢复可用 |
| M02-TC-42 | RC-M02-32 | 正向 | PASS | 顶栏展示当前项目名称并提供项目切换入口；保存后即时刷新，且**不**重新请求 `GET /api/context`（DD-M02-06/C-04） |
| M02-TC-43 | RC-M02-32 | 反向 | PASS | 项目数为 0 时顶栏回落展示默认项目名称常量「AI新一代核心系统」（不再展示「未选择项目」），且服务端不因此创建项目 |
| M02-TC-45 | RC-M02-05/06/07 | 反向 | PASS | 真实库端到端：存量 `NULL`、`''`、`','` 三种取值读取均回落默认五阶段；不修改直接保存均成功（无 `M02-E003` 误报，`M01-E006` 无），保存后 `stages` 落库为默认五阶段文本；前端侧：回落结果渲染 5 行（4 锁定 + 1 可编辑）、直接保存提交四项字段齐全 |

## 4. 验证中发现并修复的偏差

| 偏差 | 条款 | 处理 |
|---|---|---|
| 前端名称校验失败仅写入行内 `.field-error`，未给出浮动提示 | RC-M02-31（「校验失败与保存结果必须以浮动提示形式给出，且不得阻断页面其它操作」）、DD-M02-10 | 已修复：`pages/ProjectInfo.js` 改为 `reportValidation()`，同时调用既有 `showToast` 浮动提示并保留行内错误位；TC-41 由此通过 |

除此之外未发现与需求或设计不符的实现缺陷。

## 5. 未执行（跨模块）与部分验证项

以下 5 项用例的判定入口分属 M27（会话创建与上下文组装）与 M10（Agent 配置的分类列表），本仓库不含这两个模块的页面与数据映射（详细设计 DQ-M02-19 已登记）。本模块侧可验证部分均已执行并通过，端到端断言须待相应模块就绪：

| 用例 | 待联调部分 | 原因 |
|---|---|---|
| M02-TC-21 | 重新打开既有会话仍为创建时快照 | 判定入口为 M27 会话页面；M02 侧「无写入路径」已证 |
| M02-TC-22 | 新建会话上下文包含目标与范围 | 注入组装与文案属 M27（DD-M02-13、DQ-M02-03） |
| M02-TC-23 | 新建会话上下文不含人工维护配置 | 同上；人工配置清单未穷举（DQ-M02-02） |
| M02-TC-33 | 分类列表随即出现同名分类 | 分类列表渲染属 M10 |
| M02-TC-34 | 分类列表不再出现该分类 | 分类列表渲染属 M10 |

## 6. 基线保全说明

- `project_info`：破坏性用例前后的快照回放已由「还原校验」用例断言（2 行、两行名称/updated_at/阶段与原值逐字一致）。
- `agent_definition` / `agent_category`：TC-44 的临时 Agent 行已删除，44 行与分类分布与原值一致；TC-14/16/24 的迁移用例按 RC-M02-09 直接作用于真实 Agent 行，结束后分类名已还原。
  - **已知残留**：`TestVerification`（测试验证Agent）一行的 `updated_at` 因迁移用例被刷新为 `2026-09-22 08:48:16`（其分类值仍为 `测试验证`）。该行是测试用 Agent，除时间戳外无内容变化；原始时间戳无快照可还原，已登记待解决问题清单。
- 会话类表（`ai_conversation`/`ai_message`/`agent_context`）：指纹在验证前后一致，未被任何验证动作触碰。

## 7. 证据清单

| 文件 | 内容 |
|---|---|
| `target/m02tools/verify-result.json` | 接口+真实库 27 项（含 TC-27 的 PENDING 占位） |
| `target/m02tools/verify-empty-result.json` | 破坏性用例 6 项（TC-27 四段 + 还原校验 + TC-43） |
| `target/m02tools/verify-ui-result.json` | 前端交互 16 项 |
| `target/m02tools/verify-tc45-result.json` | TC-45 真实库端到端 3 项 |
| `target/m02tools/verify-tc44-result.json` | TC-44 数据侧 2 项 |
| `target/m02tools/verify-crossmod-result.json` | 跨模块用例本模块侧 11 项（5 PARTIAL + 5 NOT-EXECUTABLE + 1 基线校验） |
| `target/m02tools/app.log` | 服务启动与故障注入日志 |
| `target/m02tools/*.mjs`、`Db.java`、`Backup.java` | 验证脚本与 JDBC 辅助（非交付物） |

## 8. 本次改动文件（代码生成阶段）

后端 `src/main/java/com/musemvp/coreagent/`：`api/ProjectController.java`、`api/dto/ProjectDetailVO.java`、`domain/AgentDefinition.java`、`repository/AgentDefinitionRepository.java`、`repository/ProjectInfoRepository.java`、`service/ProjectService.java`、`service/ProjectStageService.java`、`service/AgentCategorySyncService.java`、`service/ContextService.java`、`support/StageCatalog.java`、`support/ErrorCode.java`

前端 `src/main/resources/static/`：`js/pages/ProjectInfo.js`、`js/core/store.js`、`js/layout/AppHeader.js`、`js/main.js`、`css/app.css`
